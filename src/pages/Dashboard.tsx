import Sidebar from "@/components/shared/Sidebar";

function Dashboard() {
    return (
        <section className="h-screen">
            <Sidebar />
            
        </section>
    );
}

export default Dashboard;
